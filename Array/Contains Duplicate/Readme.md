# Contains Duplicate 
# Explanation 
## Intuition 
Instead of comparing every element we compare the length of hash set and the given array if the sizes are different then a duplicate exists
## Algorithm 
Compare the length of hash set and the given array
## Complexity 
Time complexity : O(n)

Space complexity : O(n)
